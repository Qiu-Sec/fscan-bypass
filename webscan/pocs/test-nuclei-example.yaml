id: test-nuclei-example
info:
  name: Test Nuclei Example Template
  author: taskmgr-dev
  severity: info
  description: |
    This is a test template to demonstrate Nuclei format support in taskmgr.
    It will be automatically converted to taskmgr format during loading.
  reference:
    - https://github.com/shadow1ng/taskmgr

http:
  - method: GET
    path:
      - "{{BaseURL}}/robots.txt"

    matchers:
      - type: word
        words:
          - "User-agent"
          - "Disallow"
        condition: and

      - type: status
        status:
          - 200
